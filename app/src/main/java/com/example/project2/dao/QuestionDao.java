package com.example.project2.dao;

import androidx.room.Dao;
import androidx.room.Delete;
import androidx.room.Insert;
import androidx.room.Query;


import java.util.List;

/*
@Dao
public interface QuestionDao {

    @Query("SELECT * FROM questiondata")
    List<QuestionData> getAll();

    @Query("SELECT * FROM questiondata WHERE number= :number")
    List<QuestionData> getListByNumber(int number);

    @Insert
    void insert(QuestionData questionData);

    @Delete
    void delete(QuestionData questionData);


}
 */
